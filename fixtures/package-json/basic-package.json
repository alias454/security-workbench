{
  "name": "fixture-basic-package",
  "version": "1.0.0",
  "private": true,
  "description": "Fake package manifest fixture for Security Workbench.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "left-pad": "1.3.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
