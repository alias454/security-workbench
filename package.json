{
  "name": "security-workbench",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "typecheck:test": "pnpm -r --if-present typecheck:test"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1"
  }
}
